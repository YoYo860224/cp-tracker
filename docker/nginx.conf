events {}
http {
    server {
        listen 80;
        location /cp-tracker/api/ {
            proxy_pass http://be:8080/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
        location / {
            proxy_pass http://fe:80/;
        }
    }
}
